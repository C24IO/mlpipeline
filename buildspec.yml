version: 0.2

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"

phases:
  install:
    commands:
       - echo Not installing
      # - command
  #pre_build:
    #commands:
      # - command
      # - command
  build:
    commands:
       - echo Not building
      # - command
  post_build:
    commands:
       - echo No post build
      # - command
artifacts:
  files:
     - '**/*'
    # - location
  #discard-paths: yes
  #base-directory: location
